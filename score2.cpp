#include <stdio.h>
#include <stdlib.h>

#define SIZE 5
 
#pragma warning(disable: 4996 4326 6031)

void main()
{
    int 형 변수 SIZE 개의 배열 scores를 정의
 
    for (int i)
        scores[i]에 난수의 100를 저장;
    for (int i)
        "scores[%d] = %d\n"
}