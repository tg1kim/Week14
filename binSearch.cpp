#include <stdio.h>

#pragma warning(disable: 4996 4326 6031)

#define SIZE 16

void main()
{
	배열 grade에 { 2,6,11,13,18,20,22,27,29,30,34,38,41,42,45,47 }로 초기화
	"탐색할 값을 입력하시오: "
	key에 일고
	"탐색 결과 = %d\n"
}

int binarySearch(int list[], int n, int key)
{
	left, rght
	while (범위 존재) {					// 탐색 구간이 존재하면
   		middle  	// 중간 원소 결정
   		if (성공)			// 탐색 완료
			return 
		else if ()		// key는 중간 원소보다 오른쪽에
			left 				// 새로운 값으로 low 설정
		else 								// key는 중간 원소보다 왼쪽에
			rght 				// 새로운 값으로 high 설정
	}   
	return 
}

